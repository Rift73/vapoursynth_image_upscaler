# Core GUI dependency
PySide6>=6.5.0

# Optional: Rich progress bars in worker mode
rich>=13.0.0
vsjetpack

# For animated GIF output
Pillow>=9.0.0
numpy>=1.20.0


# VapourSynth dependencies (install via vsrepo or your VS package manager)
# It is advised to install all vsjetpack plugins
# These are listed for reference but typically not pip-installable:
# - vapoursynth (you can install with pip as well)
# - vsmlrt